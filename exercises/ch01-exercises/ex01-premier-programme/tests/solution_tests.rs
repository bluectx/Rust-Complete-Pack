#[cfg(test)]
mod tests {
    #[test]
    fn test_program_compiles() {
        // Ce test vérifie simplement que le programme compile
        // Pour un vrai test, on pourrait capturer stdout
        assert!(true);
    }
}

